#pragma once

/*
#include "LightBase.h"


#define MAX_CASCADES 5


EXPORT(struct,  DirectionalLight) : public Light
{
public:
	FVector3 Direction;

	void SnapToFrustum(Camera* cam);
	void Update();
	void CreateFlare();
	void DrawFlare();

	DirectionalLight(const FVector3& direction, Color color, float intensity);

	FMatrix3 localSpace;
	AABB aabb;

private:
	float boundingIndex;

};*/