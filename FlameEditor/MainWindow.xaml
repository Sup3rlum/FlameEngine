<Window x:Class="FlameEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlameEditor"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        mc:Ignorable="d"
        Title="MainWindow" Width="2560" Height="1440" WindowState="Maximized" Loaded="Window_Loaded" Closed="Window_Closed">
    <DockPanel Name="mainDockPanel" Background="#2a2a2a"> 
        <Menu DockPanel.Dock="Top" Background="#2a2a2a" Foreground="LightGray" Margin="2">
            <MenuItem Header="_File">
                <MenuItem Header="New project"/>
                <MenuItem Header="Open project"/>
                <MenuItem Header="Save project"/>
            </MenuItem>
        </Menu>
        <ToolBarTray DockPanel.Dock="Top" Background="#2a2a2a">
            <ToolBar Background="#2a2a2a">
                <Button Content="New" Foreground="LightGray"/>
                <Button Content="Open" Foreground="LightGray"/>
                <Button Content="Save" Foreground="LightGray"/>
            </ToolBar>
        </ToolBarTray>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Background="#FFFA67">
                <TextBlock Text="Build Successful" Foreground="Black" Margin="1" FontSize="12"/>
            </StatusBarItem>
        </StatusBar>

        <DockPanel DockPanel.Dock="Right">

            <DockPanel DockPanel.Dock="Top" Width="400" Height="700">
                <TextBox DockPanel.Dock="Top" Background="#2b2b2b" Margin="5 5 5 0" BorderBrush="#606060" Height="30" Foreground="White" FontSize="13" Padding="4"/>
                <TreeView Name="sceneTreeView" Background="#2a2a2a" BorderThickness="0" Margin="5" Padding="0 5 0 0">

                    <TreeView.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                Color="#007ACC" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}"
                Color="Black" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}"
                Color="LightGray" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}"
                Color="Black" />

                    </TreeView.Resources>

                </TreeView>
            </DockPanel>

            <DockPanel DockPanel.Dock="Bottom" Width="400">
                <TextBlock DockPanel.Dock="Top" Text="Build Successful" Background="#FFFA67" Foreground="Black" Margin="1" FontSize="12"/>
                <TreeView Name="editorTreeView" Background="#2a2a2a" BorderThickness="0" Margin="5" Padding="0 5 0 0">

                    <TreeView.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                    Color="#007ACC" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}"
                    Color="Black" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}"
                    Color="LightGray" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}"
                    Color="Black" />

                    </TreeView.Resources>

                </TreeView>
            </DockPanel>

        </DockPanel>

        <DockPanel DockPanel.Dock="Bottom" Height="300" Background="#303030">
            <TextBlock Text="Assets" FontSize="13" Padding="10 4 0 4" Foreground="White" DockPanel.Dock="Top" Background="#404040"/>
            <Border BorderBrush="#606060" BorderThickness="1" Margin="5">
                <WrapPanel Background="#232323">
                </WrapPanel>
            </Border>
        </DockPanel>

        <WindowsFormsHost Name="wfhPanel" Margin="10" >
            <wf:Panel x:Name="enginePanel"/>
        </WindowsFormsHost>

    </DockPanel>
</Window>
